#include <stdio.h>
int main(){
	int sval, eval, rval;
	
	printf("시작값을 입력\n");scanf("%d",&sval);
	printf("종료값을 입력\n");scanf("%d",&eval);
	rval = 0;
	int index = sval 
	while(index >= sval && index <= eval){//수행 조건 식 
		
	}
	rval = rval+sval; sval++;
	rval = sval++;
}
